apiVersion: sonic.net/v1
kind: UpgradeWorkflow
metadata:
  name: complete-upgrade-workflow
spec:
  steps:
    # Step 1: Download main image
    - name: download-main-image
      type: download
      params:
        url: "http://httpbin.org/robots.txt"
        filename: "/tmp/main-image.bin"
        md5: "46fd03688e49c6b6b0a2b7d3553c1e42"
        version: "2.0.0"
    
    # Step 2: Download backup image
    - name: download-backup-image
      type: download
      params:
        url: "http://httpbin.org/robots.txt"
        filename: "/tmp/backup-image.bin"
        md5: "46fd03688e49c6b6b0a2b7d3553c1e42"
        version: "2.0.0-backup"
    
    # Future steps (not implemented yet):
    # - name: verify-disk-space
    #   type: diskCheck
    #   params:
    #     requiredSpace: "2GB"
    # 
    # - name: install-image
    #   type: install
    #   params:
    #     source: "/tmp/main-image.bin"